package item.identitydisk;

import square.TronObject;
import effect.Effect;


/**
 * The effect the identity disk has on an {@link TronObject object}.
 *
 */
public class IdentityDiscEffect implements Effect {

	@Override
	public void execute(TronObject object) {
		// Sadly, an identity disc does nothing ... 
	}
	
}
